export { cn } from "cn"

/** Full-bleed on phones; contained card from sm up. */
export const screenPanelClass =
  "bg-card px-4 py-5 sm:rounded-xl sm:border sm:p-6"

/** Horizontal inset for titles and controls when the shell is edge-to-edge. */
export const screenPadClass = "px-4 sm:px-0"
